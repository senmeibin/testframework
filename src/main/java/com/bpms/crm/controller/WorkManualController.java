package com.bpms.crm.controller;import com.bpms.cmn.controller.FileUploadController;import com.bpms.cmn.utility.AccessUtils;import org.apache.commons.lang3.StringUtils;import org.springframework.stereotype.Controller;import org.springframework.ui.Model;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RequestMethod;import java.io.IOException;/** * 工作手册控制器类 */@Controller@RequestMapping("/crm/workmanual")public class WorkManualController extends FileUploadController {    /**     * 工作手册上传/下载列表画面显示     *     * @return 工作手册上传/下载列表画面     * @throws IOException     * @throws IllegalAccessException     * @throws InstantiationException     */    @Override    @RequestMapping(value = {"list", ""}, method = RequestMethod.GET)    public String list(Model model) throws IllegalAccessException, IOException, InstantiationException {        String isReadonly = this.getRequest().getParameter("isReadonly");        //工作手册上传必须具有管理员权限        if (StringUtils.equals(isReadonly, Boolean.FALSE.toString()) || StringUtils.isEmpty(isReadonly)) {            if (!(AccessUtils.isSystemManagement() || AccessUtils.isItSupport())) {                return "error/401";            }        }        this.initOptionList(model);        //注：各个模块动态加载，返回固定的JSP路径        return "crm/workmanual/WorkManualList";    }}